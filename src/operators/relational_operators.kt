package operators

fun main(){

    val str = "Hello Kotlin"

    println(str.length == 11)
    println(str.length != 11)
    println(str.length > 11)
    println(str.length >= 11)
    println(str.length < 11)
    println(str.length <= 11)
}